# .env.example
# Copy this file to .env and fill your Brave API key below

BRAVE_API_KEY=YOUR_BRAVE_API_KEY_HERE

# The port for the MCP server to listen on internally (default 8000)
PORT=8000

# Optional: the host port to map to container port 8000 when running Docker
HOST_PORT=8000